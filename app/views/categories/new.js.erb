$(".modal-header").append("abc")
$("#modal-window").find(".modal-content").html("<%= j (render 'form') %>");
$("#modal-window").modal();

$("#new_category").validate({
    rules: {
      "category[name]": "required",
      "category[description]": "required"
    },
    messages: {
      "category[name]": 'please enter name',
      "category[description]": 'please enter description'
    }
});
